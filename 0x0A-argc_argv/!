#include "main.h"
#include <stdio.h>
/**
 *main -  the main functio
 *@argc: the count arg
 *@argv: the vector
 *Return: 0
 */
int main(int argc, char *argv[])
{
int p, j, mul;
if (argc != 3)
{
printf("Error\n");
return (1);
}
else
{
p = atoi(agrv[1]);
j = atoi(agrv[2]);
mul = p * j;
}
printf("mul=%d\n", mul);
return (0);
}

