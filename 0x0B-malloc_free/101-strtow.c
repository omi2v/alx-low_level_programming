#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 *strtow - function to str
 *@str: the first char
 *Return: something
 */
char **strtow(char *str)
{
	char *s;
unsigned int t;
	if (str == NULL || str == "")
		return (NULL);
	if (str == NULL)
		return (0);
	for (t = 0; str[s] != NULL; t++)
	{
		str[s] = "";
}
	return (NULL);
	}
