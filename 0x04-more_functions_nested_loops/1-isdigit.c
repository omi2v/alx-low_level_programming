#include "main.h"
/**
*_isdigit - return 1 if c is a digit 
*@c: takes digit  
*Return: 1 for digit 0 for not 
*/
int _isdigit(int c)
{
for (c == '0'; c <= '9'; c++)
	if (c==0 && c<=9)
{
return (1);
}
else
{
return (0);
}
